import 'package:flutter/material.dart';

class ConnectionUnavailableWidget extends StatelessWidget {
  const ConnectionUnavailableWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
